<div class="page-header">
  <h3>Like #<%= @like.id %></h3>
</div>

<div class="row">
  <div class="col-md-12 mb-2">
    <dl class="dl-horizontal">
      <dt>Artwork</dt>
      <dd>
        <% if @like.artwork.present? %>
          <a href="/artworks/<%= @like.artwork_id %>">
            <%= @like.artwork.caption %>
          </a>
        <% end %>
      </dd>

      <dt>User</dt>
      <dd>
        <% if @like.user.present? %>
          <a href="/users/<%= @like.user_id %>">
            <%= @like.user.email %>
          </a>
        <% end %>
      </dd>

    </dl>

    <div class="btn-group btn-group-justified">
      <a href="/likes" class="btn btn-primary">
        Back
      </a>
      <% if current_user == @like.user %>
        <a href="/likes/<%= @like.id %>/edit" class="btn btn-warning">
          Edit
        </a>
        <a href="/delete_like/<%= @like.id %>" class="btn btn-danger" rel="nofollow">
          Delete
        </a>
      <% end %>
    </div>
  </div>
</div>
